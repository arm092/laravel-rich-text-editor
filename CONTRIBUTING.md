# Contributing

Use English for code, comments, tests, commits, pull requests, and documentation. Create focused commits and include tests for behavioral changes.

```bash
composer install
npm install
composer test
npm run typecheck
npm test
npm run build
npm run check:bundles
```

Do not expose internal editor-engine objects through the public API without an approved API design and compatibility plan.
